---
sidebar_position: 1
title: Calendar
---
import CalendarDemo from '@/demo/CalendarDemo'
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';

Calendar

<CalendarDemo />

## Installation

<DynamicCodeBlock lang="bash" code={`lunar add calendar`} />

## Usage

<DynamicCodeBlock lang="tsx" code={`import { Calendar } from '@lunar-kit/core';

export default function Example() {
  return (
    <Calendar
          value={date}
          onValueChange={setDate}
          variant="date"
        />
  );
}`} />

## Example

<DynamicCodeBlock lang="tsx" code={`<Calendar
          value={date}
          onValueChange={setDate}
          variant="date"
        />`} />

---

Check [source](https://github.com/dimsmaul/lunar-kit/tree/main/packages/core/src/components/ui/calendar.tsx).
