---
sidebar_position: 1
title: Tabs
---
import TabsDemo from '@/demo/TabsDemo'
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';

Tabs component for your UI needs.

<TabsDemo />

## Installation

<DynamicCodeBlock lang="bash" code={`lunar add tabs`} />

## Usage

<DynamicCodeBlock lang="tsx" code={`import { Tabs } from '@lunar-kit/core';

export default function Example() {
  return <Tabs>Example</Tabs>;
}`} />

## Examples

### Basic

<DynamicCodeBlock lang="tsx" code={`<Tabs>Example</Tabs>`} />

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'pill'` \| `'underline'` | `'underline'` | Visual style variant |
| `value` | `string` | — |  (required) |
| `onValueChange` | `(value: string) => void` | — |  (required) |
| `children` | `React.ReactNode` | — |  (required) |

---

Check [source](https://github.com/dimsmaul/lunar-kit/tree/main/packages/core/src/components/ui/tabs.tsx).
