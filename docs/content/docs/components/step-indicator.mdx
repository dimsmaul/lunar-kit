---
sidebar_position: 30
title: Step Indicator
---

import StepIndicatorDemo from '@/demo/StepIndicatorDemo';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';

A step progress indicator component with horizontal and vertical orientations, supporting completed, active, and pending states.

<StepIndicatorDemo />

## Installation

<DynamicCodeBlock lang="bash" code={`lunar add step-indicator`} />

## Usage

<DynamicCodeBlock lang="tsx" code={`import { StepIndicator } from '@/lunar-kit/components/step-indicator';`} />

### Basic Usage

<DynamicCodeBlock lang="tsx" code={`const steps = [
  { title: 'Account', description: 'Create your account' },
  { title: 'Profile', description: 'Set up your profile' },
  { title: 'Review', description: 'Review your information' },
  { title: 'Confirm', description: 'Confirm and submit' },
];

<StepIndicator steps={steps} currentStep={1} />`} />

### Vertical Orientation

<DynamicCodeBlock lang="tsx" code={`<StepIndicator
  steps={steps}
  currentStep={2}
  orientation="vertical"
/>`} />

### Without Description

<DynamicCodeBlock lang="tsx" code={`const steps = [
  { title: 'Step 1' },
  { title: 'Step 2' },
  { title: 'Step 3' },
];

<StepIndicator steps={steps} currentStep={0} />`} />

## API Reference

### StepIndicator

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `steps` | `Step[]` | Required | Array of steps |
| `currentStep` | `number` | Required | Current step index (0-based) |
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Layout orientation |
| `className` | `string` | — | Additional classes |

### Step

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `title` | `string` | Required | Step title |
| `description` | `string` | — | Step description |