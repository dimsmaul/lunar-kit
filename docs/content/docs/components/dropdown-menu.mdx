---
sidebar_position: 1
title: DropdownMenu
---
import DropdownMenuDemo from '@/demo/DropdownMenuDemo'
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';

DropdownMenu component for your UI needs.

<DropdownMenuDemo />

## Installation

<DynamicCodeBlock lang="bash" code={`lunar add dropdown-menu`} />

## Usage

<DynamicCodeBlock lang="tsx" code={`import { DropdownMenu } from '@lunar-kit/core';

export default function Example() {
  return <DropdownMenu />;
}`} />

## Examples

### Basic

<DynamicCodeBlock lang="tsx" code={`<DropdownMenu />`} />

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default'` \| `'destructive'` | `'N/A'` | Visual style variant |
| `size` | `'sm'` \| `'md'` \| `'lg'` | `'md'` | — |
| `shadow` | `'none'` \| `'sm'` \| `'md'` \| `'lg'` | `'md'` | — |
| `defaultVariants` | `'variant'` \| `'disabled'` | `'N/A'` | — |
| `variants` |  | `'N/A'` | — |
| `disabled` | `'true'` \| `'false'` | `'N/A'` | — |

---

Check [source](https://github.com/dimsmaul/lunar-kit/tree/main/packages/core/src/components/ui/dropdown-menu.tsx).
